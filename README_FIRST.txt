If you got a config.json file with the config generator tool or you already have a config.json file before,
delete the config.json here and replace it with this one.


Do not send this file(README_FIRST.txt) to the host after reading it.